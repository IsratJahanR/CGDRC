[build]
  command = "npm run build"
  publish = "dist"  # For Vite, default output folder is 'dist'
  
[dev]
  command = "npm start"
  port = 3000
  targetPort = 3000

# Optional: Redirect all routes to index.html (for SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
